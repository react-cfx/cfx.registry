import { send } from 'micro'
import reTryRequest from '../reTryRequest'

import packages from './packages'

export default (req, res) =>

  data = await reTryRequest (preUrl) => preUrl

  send res, 200
  , data

export {
  packages
}
